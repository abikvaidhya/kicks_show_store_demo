name: demo_task
description: "priority soft demo task"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons:

  #  firebase
  firebase_core:
  firebase_storage:
  firebase_cloud_firestore:

  #  state management
  get:

  #    image
  cached_network_image:

  #    device
  flutter_screenutil:

  # google font
  google_fonts:

  # slider
  flutter_xlider:  # range slide

  #    misc
  flutter_slidable: # slide widget feature
  path_provider_android:
  shimmer: # shimmer effect for loader
  flutter_secure_storage:  # secure storage for local storage
  blurrycontainer: ^2.1.0

#  fluttertoast: # toast for UI

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true


  assets:
    - assets/images/
    - assets/backgrounds/

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
